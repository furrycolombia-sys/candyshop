# Code Review Report

**Generated:** 2026-04-20 23:21:50
**Branch:** develop
**Scope:** apps/
**Review ID:** `2026-04-20_23-21-50`
**Agents:** 11/11 completed

---

## Summary

| Severity      | Count  |
| ------------- | ------ |
| 🔴 Critical   | 0      |
| 🟠 Warning    | 45     |
| 🟡 Suggestion | 22     |
| ℹ️ Info       | 1      |
| **Total**     | **68** |

---

## 🟠 Warnings

### Architecture (ARCH)

**[ARCH-001]** `apps/admin/src/features/users/presentation/pages/UsersPageContent.tsx:9`
Cross-feature import: `users` imports `useLogExport` directly from `audit` feature's application layer.
**Fix:** Move `useLogExport` to `shared/application/hooks` or expose via `audit/index.ts`.

**[ARCH-002]** `apps/payments/src/features/assigned-orders/application/hooks/useAssignedOrders.ts:6`
Cross-feature import: `assigned-orders` imports `fetchAssignedOrders` from `received-orders` infrastructure.
**Fix:** Extract `fetchAssignedOrders` into `payments/src/shared/infrastructure/orderQueries.ts`.

**[ARCH-003]** `apps/payments/src/features/assigned-orders/presentation/pages/AssignedOrdersPageContent.tsx:9`
Multiple cross-feature imports: `assigned-orders` imports `useOrderActions`, `SellerAction`, and `ReceivedOrderCard` from `received-orders`.
**Fix:** Merge both features OR extract shared types/components to `payments/shared/`.

**[ARCH-004]** `apps/studio/src/features/products/presentation/pages/ProductListPage.tsx:8`
Cross-feature import: `products` imports `useDelegateCountsByProduct` and `SELLER_ADMINS_READ_PERMISSION` from `seller-admins`.
**Fix:** Expose via `seller-admins/index.ts` or lift to page level as props.

**[ARCH-005]** `apps/admin/src/shared/application/hooks/useRecentActivity.ts`
Dead code: unused hook in `shared/` — the real one used is in `features/dashboard/application/hooks/`.
**Fix:** Delete `shared/application/hooks/useRecentActivity.ts`.

**[ARCH-006]** `apps/admin/src/shared/infrastructure/recentActivityQueries.ts`
`fetchRecentActivity` is in `shared/infrastructure` but used only by the dashboard feature.
**Fix:** Move to `features/dashboard/infrastructure/recentActivityQueries.ts`.

**[ARCH-007]** Multiple paths across all apps
Most feature layers lack `index.ts` barrel exports, enabling deep internal imports.
**Fix:** Add `index.ts` barrel exports per layer where cross-layer imports actually occur.

---

### SOLID

**[SOLID-001]** `apps/admin/src/features/users/application/hooks/useUserPermissions.ts` (and `useTogglePermission.ts`, `useApplyTemplate.ts`, `useUserDelegates.ts`)
Application-layer hooks call `fetch()` directly instead of delegating to infrastructure functions.
**Fix:** Extract raw `fetch` calls into `infrastructure/api/` functions; hooks call those.

**[SOLID-002]** `apps/payments/src/features/checkout/presentation/components/SellerCheckoutCard.tsx:87-97`
Component has 5 responsibilities: accordion UI, payment method selection, buyer form state, validation logic, and submit callback.
**Fix:** Extract form validation into a dedicated hook/utility in the application layer.

**[SOLID-003]** `apps/admin/src/features/users/presentation/components/UserTable.tsx:17`
`UserTableProps` has 15 flat props spanning selection, filtering, pagination, data, and export.
**Fix:** Group into sub-objects: `filterState`, `selectionState`, `paginationState` (similar to `SellerCheckoutContent`).

**[SOLID-004]** `apps/studio/src/features/products/presentation/components/InlineEditor/SectionCard.tsx:104`
`renderItems()` inline switch dispatcher is embedded inside `SectionCard`, violating Open/Closed.
**Fix:** Extract to a `SectionItemsRenderer` component (mirror the store's `SectionRenderer` pattern).

---

### DRY

**[DRY-001]** `apps/store/src/features/products/presentation/components/product-detail/`
Leftover duplicate of `ProductDetail/` (100% identical files) from an incomplete kebab-case rename.
**Fix:** Delete the `product-detail/` folder — `ProductDetailPage.tsx` imports only from `ProductDetail/`.

**[DRY-002]** `apps/store/src/features/products/presentation/components/product-detail/lucideIconMap.ts`
Duplicate of `presentation/utils/lucideIconMap.ts` (100% identical).
**Fix:** Remove as part of DRY-001 cleanup.

**[DRY-003]** `apps/store/src/features/cart/application/useSellerProfiles.ts` + `.test.ts`
Stale duplicate of `application/hooks/useSellerProfiles.ts` (root-level uses direct Supabase client; `hooks/` version uses `useSupabase()` and is canonical).
**Fix:** Delete root-level `useSellerProfiles.ts` and its test.

**[DRY-004]** `apps/store/src/features/cart/application/useFlyToCart.ts` + `.test.ts`
Stale duplicate of `application/hooks/useFlyToCart.ts` (identical files).
**Fix:** Delete root-level `useFlyToCart.ts` and its test.

---

### Component Patterns (COMP)

**[COMP-001]** `apps/studio/src/features/products/presentation/components/inline-editor/` (~47 files)
Full duplicate of `InlineEditor/` directory. Production imports use only `InlineEditor/`.
**Fix:** Remove the `inline-editor/` (kebab-case) directory entirely.

**[COMP-002]** Multiple files in `apps/payments/` and `apps/store/`
`'use client'` directive placed after `/* eslint-disable */` block comments (10 files affected).
**Fix:** Move `'use client'` to line 1, then place eslint-disable comments after it.

**[COMP-003]** `apps/studio/.../InlineEditor/LangTextarea.tsx` and `PriceInput.tsx`
Exported components use inline object type destructuring instead of named `ComponentNameProps` interface.
**Fix:** Extract inline type to named `LangTextareaProps` / `PriceInputProps` interface.

**[COMP-004]** Multiple `InlineEditor` components (SectionCard, SectionItemsAccordion, etc.)
`as any` used for react-hook-form dynamic template-literal field paths. All usages are guarded by `eslint-disable` with documented justification — this is an accepted pattern.
**Note:** Already documented inline; no change required unless RHF types improve.

---

### Naming Conventions (NAME)

**[NAME-001]** `apps/store/.../components/ProductDetail/`
PascalCase folder coexists with `product-detail/` — both should be collapsed to one.
**Fix:** Delete `product-detail/` (resolved by DRY-001) or rename `ProductDetail/` to `product-detail/`.

**[NAME-002]** `apps/studio/.../components/InlineEditor/`
PascalCase `InlineEditor/` folder should be `inline-editor/`. Both coexist.
**Fix:** Delete the `inline-editor/` duplicate (resolved by COMP-001) and rename `InlineEditor/` → `inline-editor/`.

**[NAME-003]** `apps/admin/.../components/Delegates/`
PascalCase grouping folder — should be `delegates/`.
**Fix:** Rename `Delegates/` → `delegates/` and update imports.

**[NAME-004]** `apps/store/.../ProductDetail/Sections/`
Sub-grouping folder uses PascalCase `Sections/` — should be `sections/`.
**Fix:** Rename to `sections/`.

**[NAME-005]** `apps/studio/.../inline-editor/sectionItemTypes.ts`
Type file uses camelCase — should be `SectionItemTypes.ts`.
**Fix:** Rename after directory consolidation.

**[NAME-006]** `apps/studio/src/features/products/application/` (5 hook files)
Hook files at `application/` root AND in `application/hooks/` (duplicate).
**Fix:** Remove root-level duplicates; canonical is `application/hooks/`.

**[NAME-007]** `apps/admin/.../audit/application/`, `apps/store/.../products/application/`, `apps/store/.../cart/application/`
Same root-vs-hooks duplication pattern as NAME-006 (9 more files).
**Fix:** Delete root-level stale copies; update any imports that still reference them.

---

### Bug Detection (BUG)

**[BUG-001]** `apps/store/src/features/cart/application/CartContext.tsx:118-137`
Double cookie write on every mutation: the eager synchronous write in callbacks uses stale pre-dispatch `state.items`; the `useEffect` write uses correct post-dispatch state.
**Fix:** Remove `persistCartCookie` calls inside `addItem`, `removeItem`, `updateQuantity` — let the `useEffect` be the single source of truth.

**[BUG-003]** `apps/store/src/features/products/presentation/components/ProductFilters.tsx:44-50`
Stale closure risk in debounced URL state sync-back effect (known trade-off, documented).
**Note:** Acceptable per current debounce pattern; consider `useRef` guard if race conditions manifest.

**[BUG-004]** `apps/admin/src/features/users/application/hooks/useUserPermissionsBatch.ts:8-19`
`Promise.all` rejects entire export if any single user's permissions query fails.
**Fix:** Use `Promise.allSettled` and filter fulfilled results; rejected users get empty arrays.

---

### Tailwind/Styling (STYLE)

**[STYLE-005]** `apps/landing/src/features/home/presentation/components/FeaturesSection.tsx:134`
Three arbitrary font-size values: `text-[1.55rem]`, `sm:text-[1.8rem]`, `lg:text-[2rem]`, `leading-[0.94]`.
**Fix:** Define a semantic `text-category-title` class in `globals.css` under `@layer utilities`.

---

### Testing (TEST)

**[TEST-001]** `apps/payments/src/features/assigned-orders/` — **Zero test coverage** on newly added GH-119 feature.
**Fix:** Add `useAssignedOrders.test.tsx`, `AssignedOrdersPage.test.tsx` covering loading/success/error/delegate-access states.

**[TEST-002]** `apps/payments/src/features/checkout/presentation/components/` (6 files)
Security-sensitive checkout components (BuyerField, DynamicFormField, DisplayBlockRenderer, ImageBlock, DisplayBlocksSection, FormFieldsSection) have no tests.
**Fix:** Add component tests covering field types and null/empty data.

**[TEST-003]** `apps/payments/src/features/payment-methods/presentation/components/` (5 files)
Payment method editor sub-components (BlockEditor, FormSectionEditor, DisplaySectionEditor, VideoBlockEditor, FieldRow) have no tests.
**Fix:** Add interaction tests covering add/remove/edit and validation rendering.

**[TEST-004]** `apps/admin/src/features/users/presentation/components/Delegates/` (5 files)
Delegate permission management sub-components have no tests.
**Fix:** Add unit tests per component covering various permission states.

**[TEST-005]** `apps/admin/src/features/users/application/hooks/useUserPermissionsBatch.ts` and `useUserReceiptsBatch.ts`
Batch aggregation hooks with no tests — edge cases (empty, partial errors) are critical.
**Fix:** Add tests covering empty input, single user, multiple users, error states.

**[TEST-006]** `apps/payments/src/features/received-orders/presentation/components/ConfirmActionPanel.tsx`
Interactive confirmation dialog for destructive order actions has no tests.
**Fix:** Add tests for confirm/cancel callbacks and button states.

---

### Security (SEC)

**[SEC-001]** `apps/admin/src/shared/infrastructure/auditRestClient.ts:14`
Silent fallback: server-side code falls back to `NEXT_PUBLIC_SUPABASE_ANON_KEY` without warning when `SUPABASE_ANON_KEY` is missing.
**Fix:** Log a warning or throw if falling back to the `NEXT_PUBLIC_` key in server-side code.

**[SEC-002]** `.env.prod` and `.env.staging`
Production/staging Supabase project URLs committed in plaintext.
**Fix:** Move `NEXT_PUBLIC_SUPABASE_URL` values to GitHub secrets (`$secret:PROD_SUPABASE_URL`) for public repo safety.

**[SEC-003]** `apps/admin/src/app/api/admin/users/[userId]/permissions/route.ts:197,255`
POST and PUT handlers parse `request.json()` before calling `getAuthorizedAdmin()`.
**Fix:** Move `getAuthorizedAdmin()` to the top of each handler, before body parsing.

---

### Performance (PERF)

**[PERF-001]** `apps/store/src/features/cart/presentation/components/CartDrawer.tsx:142`
Inline `translators={{ t, tProducts, tTypes, tCategories }}` object recreated each render, preventing CartItemRow bailout.
**Fix:** `const translators = useMemo(() => ({ t, tProducts, tTypes, tCategories }), [t, tProducts, tTypes, tCategories]);`

**[PERF-002]** `apps/store/src/features/cart/presentation/components/CartItemRow.tsx:112,129,141`
Three inline arrow functions in onClick props recreated on every render.
**Fix:** Wrap `CartItemRow` with `React.memo` and convert handlers to `useCallback`.

**[PERF-003]** `apps/studio/src/features/products/presentation/components/InlineEditor/InlineEditor.tsx`
`@hello-pangea/dnd` imported eagerly, bundled into the initial seller editor chunk.
**Fix:** Wrap DnD-dependent components with `next/dynamic({ ssr: false })`.

---

## 🟡 Suggestions

| ID        | File                                                                               | Description                                                              |
| --------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| ARCH-008  | `apps/payments/.../ExpirationLabel.tsx:44`                                         | Inline type `{ expiresAt: string }` — define `ExpirationLabelProps`      |
| ARCH-009  | `apps/store,payments,studio/src/test/fixtures/appUrls.ts`                          | Identical `mockAppUrls` fixture in 3 apps                                |
| SOLID-005 | `apps/studio/.../EditorToolbar.tsx`                                                | Too many controls — extract grouped sub-components if it grows           |
| SOLID-006 | `InlineEditor/` + `inline-editor/` and `ProductDetail/` + `product-detail/`        | Duplicate directories (addressed by DRY/COMP fixes)                      |
| DRY-005   | `admin/adminRest.ts` + `payments/adminRestClient.ts`                               | Supabase admin REST client duplicated — consider `packages/api-server`   |
| DRY-006   | `store/products/domain/searchParams.ts` + `studio/products/domain/searchParams.ts` | Identical product filter search params                                   |
| DRY-007   | `admin/.../constants.ts` + `studio/.../DelegateList.tsx`                           | `MAX_INITIALS_LENGTH = 2` defined in two places                          |
| COMP-005  | `apps/store/.../CartItemRow.tsx`                                                   | `TranslatorFn` uses `any[]` — use `ReturnType<typeof useTranslations>`   |
| COMP-006  | `InlineEditor/SectionCard.tsx` + `checkout/DynamicFormField.tsx`                   | Internal render helpers defined inside component body                    |
| COMP-007  | `InlineEditor/SectionItemsAccordion.tsx` + `SectionItemsGallery.tsx`               | Private compound helper props typed inline                               |
| NAME-008  | `apps/payments/.../shared/domain/`                                                 | Two type files (types.ts + PaymentMethodTypes.ts) in same domain folder  |
| NAME-009  | `apps/payments/.../PaymentsSidebarNav.tsx`                                         | Local constants use SCREAMING_SNAKE_CASE (should be camelCase)           |
| BUG-005   | `apps/payments/.../checkoutQueries.ts:133-134`                                     | `priceMap.values().next().value` can be `undefined` — add explicit guard |
| STYLE-002 | `apps/studio/.../DelegateList.tsx:12,30`                                           | `EMPTY_STATE_CLASS` Tailwind constant — inline the classes               |
| STYLE-003 | `apps/admin/.../DashboardPageContent.tsx:13-14`                                    | `NO_ACTIVITY_CLASS` Tailwind constant — inline or extract component      |
| STYLE-004 | `apps/payments/.../PaymentsSidebar.tsx:13`                                         | `COLLAPSED_WIDTH_CLASS` Tailwind constant — inline                       |
| TEST-007  | `apps/studio/.../inline-editor/` (22 test files)                                   | Duplicate test files from directory duplication                          |
| TEST-008  | `apps/studio/.../products/application/*.test.ts` (10 files)                        | Duplicate tests at root vs hooks/ in studio/products                     |
| TEST-009  | `apps/landing/src/features/home/presentation/`                                     | Landing sections largely untested                                        |
| SEC-004   | `apps/payments/src/app/api/checkout/orders/route.ts`                               | No rate limiting on checkout API routes                                  |
| PERF-004  | `apps/store/.../TwoColumnSection.tsx:51-53`                                        | Inline style object created per map iteration                            |
| PERF-005  | `apps/store/.../HeroSection.tsx:141`                                               | Inline `discountLabel` arrow function — extract to `useCallback`         |

---

## ℹ️ Info

**[TEST-010]** `useSupabaseAuth` is intentionally untested across all apps (wraps external Supabase auth client). Pattern is consistent and acceptable.

---

## Pattern Discovery Highlights

### Good Patterns

- **App Router thin wrappers** — all route files correctly delegate to feature pages
- **Feature `index.ts` barrel exports** — clean public API per feature
- **No hardcoded production URLs** — all API bases read from env vars
- **Domain constants files** — used consistently for query keys and magic numbers
- **setInterval with cleanup** — all usages correctly return `clearInterval` in cleanup
- **Object URL lifecycle** — blob/object URLs for receipts correctly revoked on unmount
- **Event listener subscribe/unsubscribe** — no leaking listeners detected
- **Semantic color usage** — no hardcoded hex/palette colors in any `.tsx` file
- **`cn()` used consistently** — no template-literal className patterns
- **Container/Presenter pattern** — well applied throughout (PageContent → pure UI)
- **Auth verified via `getUser()` before `getSession()`** — correct secure pattern in all API routes
- **UUID validation before DB queries** — consistent `validateUuid()` helper usage

### Concerns

- **assigned-orders ↔ received-orders** heavy coupling (ARCH-002, ARCH-003) — candidate for feature merge
- **Incomplete migrations** — PascalCase→kebab-case renames were abandoned halfway in both store and studio
- **Root-level hook duplicates** — ongoing migration to `application/hooks/` left stale files in `application/`

---

## Action Items

- [ ] **[HIGH]** Delete `product-detail/` folder (DRY-001, DRY-002, NAME-001)
- [ ] **[HIGH]** Delete `inline-editor/` folder in studio (COMP-001, TEST-007)
- [ ] **[HIGH]** Delete stale root-level hook files (DRY-003, DRY-004, NAME-006, NAME-007)
- [ ] **[HIGH]** Fix SEC-003: move `getAuthorizedAdmin()` before body parsing in permissions route
- [ ] **[HIGH]** Fix BUG-001: remove double cookie writes in CartContext
- [ ] **[HIGH]** Fix BUG-004: use `Promise.allSettled` in `useUserPermissionsBatch`
- [ ] **[HIGH]** Add tests for assigned-orders feature (TEST-001)
- [ ] **[MEDIUM]** Fix ARCH-005/ARCH-006: delete dead `useRecentActivity` in shared, move `fetchRecentActivity` to dashboard
- [ ] **[MEDIUM]** Fix SEC-001: add warning/throw on anon key fallback in `auditRestClient`
- [ ] **[MEDIUM]** Fix PERF-001: memoize `translators` object in CartDrawer
- [ ] **[MEDIUM]** Fix PERF-003: lazy-load `@hello-pangea/dnd` components with `next/dynamic`
- [ ] **[MEDIUM]** Fix COMP-002: move `'use client'` to line 1 in 10 affected files
- [ ] **[MEDIUM]** Rename `Delegates/` → `delegates/` (NAME-003) and update imports
- [ ] **[LOW]** Inline Tailwind class constants (STYLE-002, STYLE-003, STYLE-004)
- [ ] **[LOW]** Add missing test coverage (TEST-002 through TEST-006)
- [ ] **[LOW]** Fix STYLE-005: define `text-category-title` semantic class
- [ ] **[LOW]** Fix BUG-005: add explicit priceMap empty guard in checkoutQueries
