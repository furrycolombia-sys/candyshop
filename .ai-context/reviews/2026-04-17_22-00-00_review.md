# Code Review Report — Cycle 8

**Generated:** 2026-04-17 22:00:00  
**Scope:** `apps/`  
**Review ID:** `2026-04-17_22-00-00`  
**Branch:** `develop`  
**Agents:** 11/11 completed

---

## Summary

| Severity      | Found  | Fixed  | Deferred |
| ------------- | ------ | ------ | -------- |
| 🔴 Critical   | 1      | 1      | 0        |
| 🟠 Warning    | 32     | 9      | 23       |
| 🟡 Suggestion | 18     | 3      | 15       |
| ℹ️ Info       | 2      | 0      | 2        |
| **Total**     | **53** | **13** | **40**   |

All **critical issues resolved**. All 23 deferred warnings are either large architectural refactors (ARCH, SOLID), infrastructure configuration decisions (SEC-002/003, STYLE-001), test coverage gaps (TEST-001–010), or known pre-existing issues tracked from earlier cycles.

---

## 🔴 Critical Issues

### BUG-001 — ✅ FIXED

**Null context (`ctx`) accessed unsafely inside async `.then()` callback**  
`apps/store/src/features/products/presentation/components/FeaturedRibbon.tsx:180`

`ctx` typed as `CanvasRenderingContext2D | null` — TypeScript narrowing from `if (!ctx) return` does not persist into the async `.then()` callback. Added `!ctx` guard inside the callback to match the guard already present in `draw()`.

---

## 🟠 Warnings — Fixed

### BUG-003 — ✅ FIXED

**Stock release loop not wrapped in try/catch — silent failures swallow partial releases**  
`apps/payments/src/features/checkout/infrastructure/checkoutQueries.ts:103`

Both compensating release loops (reserve-failure path and order-insert-failure path) were missing try/catch. If any individual `release_stock` RPC threw, the loop exited early leaving remaining reserved stock permanently locked until expiry. Both loops now individually catch and log release errors, ensuring all items are attempted.

### BUG-004 — ✅ FIXED

**MutationObserver callback accesses canvas after component teardown**  
`apps/store/src/features/products/presentation/components/FeaturedRibbon.tsx:93`

Added `if (cancelled) return` guard inside the MutationObserver callback — mirrors the same guard already present in the `draw()` animation function. Prevents stale-reference access during the window between `cancelled = true` and `themeObserver.disconnect()`.

### COMP-001 through COMP-005 — ✅ FIXED

**React hooks (`useMemo`) used without `'use client'` directive in 5 components**

- `apps/store/src/features/products/presentation/components/product-detail/ProductSections.tsx`
- `apps/store/src/features/products/presentation/components/product-detail/sections/AccordionSection.tsx`
- `apps/store/src/features/products/presentation/components/product-detail/sections/CardsSection.tsx`
- `apps/store/src/features/products/presentation/components/product-detail/sections/GallerySection.tsx`
- `apps/store/src/features/products/presentation/components/product-detail/sections/TwoColumnSection.tsx`

Added `"use client"` directive to `ProductSections.tsx`. As the parent, this cascades client context to all four child section components automatically, resolving all five issues with a single change. Each component used `useMemo` relying implicitly on client-component parent propagation — a fragile dependency removed by the explicit directive.

### SEC-001 — ✅ FIXED

**`adminFetchJson` leaks raw Supabase error body into server logs**  
`apps/payments/src/shared/infrastructure/adminRestClient.ts:72`

Replaced `throw new Error(await response.text())` with body-consuming-then-discarding pattern: `await response.text(); throw new Error(...)` with a sanitized message. PostgREST error bodies can include schema details, table names, constraint names, and RLS policy names — these no longer appear in server logs. Matches the pattern already correctly used in `apps/admin/src/app/api/admin/_shared/adminRest.ts`.

### DRY-003 — ✅ FIXED

**Time conversion constants (`MS_PER_SECOND`, `SECONDS_PER_MINUTE`, `MINUTES_PER_HOUR`) defined in 3 separate domains**

Added canonical constants to `packages/shared/src/constants/time.ts` (including `HOURS_PER_DAY`). Updated all 3 consuming locations to re-export from shared instead of defining local copies:

- `apps/payments/src/features/checkout/domain/constants.ts` — now re-exports from `shared/constants/time`
- `apps/admin/src/features/users/domain/constants.ts` — now re-exports from `shared/constants/time`
- `apps/store/src/features/cart/application/cartCookiePersistence.ts` — now imports from `shared/constants/time`

All consumers of these constants continue to work unchanged (re-exports maintain the same public API).

---

## 🟡 Suggestions — Fixed

### BUG-005 — ✅ FIXED

**Object URL for receipt preview not revoked when `file` prop reset externally**  
`apps/payments/src/features/checkout/presentation/components/ReceiptUpload.tsx:53`

Added `useEffect` that detects when `file` prop becomes null from the parent (without going through the component's own `handleRemove`) and revokes the stale blob URL + clears `previewUrl` state. Prevents minor memory leak and stale preview display.

### SEC-004 — ✅ FIXED

**`validateUuid` in payments adminRestClient echoes attacker-controlled input in error message**  
`apps/payments/src/shared/infrastructure/adminRestClient.ts:8`

Changed error message to generic `"Invalid UUID format"` — no longer echoes the raw input value. Eliminates log injection risk from special characters (newlines, ANSI escape codes).

### PERF-001 — ✅ FIXED

**`getPillClass` helper recreated on every render in `ProductFilters`**  
`apps/studio/src/features/products/presentation/components/ProductFilters.tsx:55`

Hoisted `getPillClass` outside the component body (it has no closure dependencies over props or state). Pure function now created once at module load rather than on every render.

---

## 🟠 Warnings — Deferred

### ARCH-001 — Deferred (architectural refactor)

**Duplicated admin REST client across `apps/payments` and `apps/admin`**  
Two near-identical implementations of Supabase Admin REST client. Extracting to `packages/shared` or `packages/api` would require coordinating across both apps and package exports. Deferred to avoid scope creep.

### ARCH-002 — Deferred (refactor)

**Inline local type definitions in API route handler**  
`apps/payments/src/app/api/checkout/payment-methods/route.ts`  
`PaymentMethodRow`, `ProductStockRow`, `PermissionRow` defined inline. Moving to domain types is correct but a refactor. No runtime impact.

### ARCH-003 — Deferred (KISS — 14 files affected)

**One-line re-export shims in `apps/store/src/shared/domain/`**  
`CategoryTypes.ts` and `categoryConstants.ts` proxy types from `shared` package. Removing the shims requires updating ~14 importing feature files. KISS principle applies — the indirection is harmless at current scale.

### BUG-002 — Deferred (minor UX, complex fix)

**`cartCleared` ref race condition in `CheckoutPageContent`**  
On component remount within same session, the `cartCleared` ref resets, potentially clearing the checkout-completed flag prematurely. Minor UX flicker only, no data integrity risk.

### NAME-001 — Deferred (known from Cycle 7)

**`CartDrawerWithItems.test.tsx` name doesn't match any source file**  
`apps/store/src/features/cart/presentation/components/CartDrawerWithItems.test.tsx`  
Merging with `CartDrawer.test.tsx` is blocked by `vi.mock()` hoisting constraints — both files mock `CartDrawer` at module scope and merging would create a circular mock conflict.

### PERF-002 — Deferred (by design)

**Client-side filtering of full product catalog on every keystroke**  
`apps/store/src/features/products/presentation/pages/ProductCatalogPage.tsx`  
`useMemo` is already in place. Server-side filtering would require API changes. Acceptable trade-off at current catalog scale.

### PERF-003 — Deferred (pragmatic)

**Inline style objects for dynamic colors create new object references per render**  
Multiple product card components. Values are stable per category — React's fast object comparison minimizes impact. Acceptable at current scale.

### SEC-002 — Deferred (infrastructure decision)

**CSP uses `'unsafe-inline'` and `'unsafe-eval'` in `script-src`**  
All 7 `next.config.ts` files. Implementing nonce-based CSP requires Next.js middleware changes and careful testing of all apps. Infrastructure-level decision outside Cycle 8 scope.

### SEC-003 — Deferred (infrastructure decision)

**`images.remotePatterns` allows all HTTPS hosts (wildcard `**`)**  
`apps/store/next.config.ts`, `apps/studio/next.config.ts`  
Restricting to specific Supabase/CDN domains requires coordinating with deployment config. Infrastructure decision.

### SOLID-001 — Deferred (refactor)

**`ProductCatalogPage` mixes data filtering logic with presentation**  
Filtering logic belongs in application layer. Functional and well-tested currently.

### SOLID-002 — Deferred (refactor)

**`BlockEditor` / `DisplayBlockRenderer` duplicate switch-per-block-type structure**  
OCP violation — adding new block types requires editing two files. A block registry pattern would fix this but is a medium-scope refactor.

### SOLID-003 — Deferred (refactor)

**`InlineHero` uses 4 separate `useTranslations` scopes (multiple concerns)**  
`apps/studio/src/features/products/presentation/components/inline-editor/InlineHero.tsx`  
Should be split into `ProductBadgesGroup` + `InlineStockField` components. Low priority.

### PAT-007 — Deferred (cleanup)

**Inconsistent `tid()` import source — some files use `"shared"`, others use local wrapper**  
Standardizing all to `import { tid } from "shared"` requires updating ~4 wrapper files and their consumers. Harmless inconsistency.

### TEST-001 — Deferred

**`ConfirmActionPanel` (security-sensitive payment approval) has no test**  
`apps/payments/src/features/received-orders/presentation/components/ConfirmActionPanel.tsx`

### TEST-002 — Deferred

**`DynamicFormField` (137 lines, complex branching) has no test**  
`apps/payments/src/features/checkout/presentation/components/DynamicFormField.tsx`

### TEST-003 — Deferred

**`CheckoutPageContent` (222 lines, checkout orchestrator) has no test**  
`apps/payments/src/features/checkout/presentation/pages/CheckoutPageContent.tsx`

### TEST-004 — Deferred

**`UserDetailPageContent` (143 lines, complex permissions UI) has no test**  
`apps/admin/src/features/users/presentation/pages/UserDetailPageContent.tsx`

### TEST-005 — Deferred

**`BlockEditor` / `DisplaySectionEditor` (payment method editors) have no tests**

### TEST-006 — Deferred

**`DisplayBlockRenderer` (checkout display blocks) has no test**  
`apps/payments/src/features/checkout/presentation/components/DisplayBlockRenderer.tsx`

### TEST-007 — Deferred

**Admin user feature components (`UserRow`, `UserHeader`, `UserDelegatesCard`, delegates/\*) have no tests**

---

## 🟡 Suggestions — Deferred

### ARCH-004 — Deferred

Missing barrel `index.ts` for layer directories in most features. Progressive improvement.

### ARCH-005 — Deferred

Hardcoded YouTube embed URL in domain layer — acceptable given URL stability.

### COMP-006 — Deferred

`RoleBadge` in admin has unnecessary `'use client'` — minor bundle concern.

### SOLID-004 / SOLID-005 — Deferred

`UsersPageContent` and `UserTable` minor SRP/ISP concerns. Functional as-is.

### STYLE-001 — Deferred (high-risk)

**Hex colors in `packages/ui/src/styles/colors.css` instead of OKLCH format**  
Converting all hex values to OKLCH across the shared color foundation requires WCAG contrast verification on all 7 apps. High-risk change deferred.

### STYLE-002 — Deferred

Arbitrary font size values in `FeaturesSection.tsx` landing page. One-off design.

### TEST-008 through TEST-010 — Deferred

Missing tests for landing `HeroSection`, studio `ImageEditBar`, admin `DashboardPageContent`.

### PAT-003 — Deferred

5 identical `features/auth/useSupabaseAuth.ts` re-export files across apps.

### PAT-004 — Deferred

URL state not consistently applied to `orders` (payments) and `seller-admins` (studio) features.

### PAT-006 — Deferred

ESLint `i18next/no-literal-string` scope could be narrowed to presentation layer only.

---

## ℹ️ Info

### PAT-001 — Noted (documentation item)

**Permission Guard Page pattern** (`*Page` = auth check, `*PageContent` = content) is consistent across 8+ pages but undocumented. Worth adding to `architecture.md`.

### PAT-002 — Noted (documentation item)

**Thin re-export wrapper pattern** at app-level `shared/` is intentional and well-established. Worth documenting in `monorepo-architecture.md`.

---

## Positive Patterns Found

- ✅ All 7 app `globals.css` are identical — 100% CSS DRY compliance via `packages/ui` import
- ✅ App Router thin wrappers: all `page.tsx` files delegate directly to feature page components
- ✅ No class-based state assertions in any test file
- ✅ Zero snapshot tests — all assertions are behavioral
- ✅ `tid()` E2E selector strategy used consistently in specs
- ✅ No hardcoded API keys or secrets in source code
- ✅ Auth token validation via `supabase.auth.getUser()` on all API routes
- ✅ Checkout stock integrity rule implemented and tested (`payment-methods/route.test.ts`)
- ✅ URL state managed via nuqs in all filterable features
- ✅ Strong DIP adherence — no direct fetch/axios calls in component files

---

## Action Items

- [x] Fix 1 critical issue (BUG-001)
- [x] Fix 9 warnings (BUG-003, BUG-004, COMP-001–005, SEC-001, DRY-003)
- [x] Fix 3 suggestions (BUG-005, SEC-004, PERF-001)
- [ ] Add tests for security-sensitive components (TEST-001: ConfirmActionPanel — highest priority)
- [ ] Add tests for checkout orchestration (TEST-002, TEST-003)
- [ ] Consider nonce-based CSP to replace `'unsafe-inline'` (SEC-002)
- [ ] Restrict `images.remotePatterns` to known trusted hosts (SEC-003)
- [ ] Convert hex colors to OKLCH in `packages/ui/src/styles/colors.css` (STYLE-001)

---

_Full report saved to: `.ai-context/reviews/2026-04-17_22-00-00_review.md`_  
_Generated by `/full-review` skill — Cycle 8_
