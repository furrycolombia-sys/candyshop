# Code Review Report

**Generated:** 2026-04-21 03:17:10
**Branch:** feat/only-currency
**Scope:** apps/admin/src/features/reports/
**Review ID:** `2026-04-21_03-17-10`
**Agents:** 11/11 completed

---

## Summary

| Severity      | Count |
| ------------- | ----- |
| 🔴 Critical   | 9     |
| 🟠 Warning    | 32    |
| 🟡 Suggestion | 8     |
| ℹ️ Info       | 19    |

**Total Issues:** 68

---

## 🔴 Critical Issues

### [BUG-001] `item.id` React key — field does not exist on `ReportOrderItem`

- **File:** `apps/admin/src/features/reports/presentation/components/ReportTable.tsx:107`
- **Description:** `order.items.map((item) => <span key={item.id}>...)` uses `item.id` as React key, but `ReportOrderItem` type has no `id` field. This causes React key warnings, potential incorrect diffing, and subtle re-render bugs.
- **Fix:** Add `id` to `ReportOrderItem` domain type OR use a composite key: `key={\`${order.id}-${item.product_id}\`}`

### [DRY-001] `ORDER_STATUS_VALUES` in `searchParams.ts` duplicates `ORDER_STATUS_LIST` from `constants.ts`

- **File:** `apps/admin/src/features/reports/domain/searchParams.ts`
- **Description:** `const ORDER_STATUS_VALUES: OrderStatus[] = ['pending', ...]` is defined inline and duplicates the exported `ORDER_STATUS_LIST` constant from `domain/constants.ts`. Violates Single Source of Truth.
- **Fix:** Remove `ORDER_STATUS_VALUES`, import and use `ORDER_STATUS_LIST` from `./constants`

### [DRY-002] Duplicated row-building logic in `exportOrdersToExcel.ts`

- **File:** `apps/admin/src/features/reports/application/utils/exportOrdersToExcel.ts:47-85`
- **Description:** The logic to build an order row (lines 47–63) is nearly identical to item-level rows (lines 68–85). Any change requires updating both blocks.
- **Fix:** Extract a shared `buildRow(cells: string[]): string` helper and reuse it

### [TEST-001] Zero unit tests for `ReportsPage`

- **File:** `apps/admin/src/features/reports/presentation/pages/ReportsPage.tsx`
- **Fix:** Create `ReportsPage.test.tsx` with render, export, and filter interaction tests

### [TEST-002] Zero unit tests for `ReportFiltersBar`

- **File:** `apps/admin/src/features/reports/presentation/components/ReportFiltersBar.tsx`
- **Fix:** Create `ReportFiltersBar.test.tsx` covering filter inputs, clear button, and URL state

### [TEST-003] Zero unit tests for `ReportTable`

- **File:** `apps/admin/src/features/reports/presentation/components/ReportTable.tsx`
- **Fix:** Create `ReportTable.test.tsx` covering table render, empty state, expandable rows

### [TEST-004] Zero unit tests for `OrderStatusBadge`

- **File:** `apps/admin/src/features/reports/presentation/components/OrderStatusBadge.tsx`
- **Fix:** Create `OrderStatusBadge.test.tsx` covering all status values and fallback

### [TEST-005] Zero unit tests for `useReportOrders` hook

- **File:** `apps/admin/src/features/reports/application/hooks/useReportOrders.ts`
- **Fix:** Create `useReportOrders.test.ts` covering query construction, URL state parsing

### [TEST-006] Zero unit tests for `exportOrdersToExcel`

- **File:** `apps/admin/src/features/reports/application/utils/exportOrdersToExcel.ts`
- **Fix:** Create `exportOrdersToExcel.test.ts` covering Excel content generation

---

## 🟠 Warnings

### Architecture

- **[ARCH-001]** `ReportTable.tsx:4` — `import { tid } from "shared"` should be `import { tid } from "@/shared/infrastructure/config/tid"` for consistency
- **[ARCH-002]** `ReportTable.tsx:6` — Relative import `"./OrderStatusBadge"` should use absolute path for cross-component clarity

### SOLID

- **[SOLID-001]** `ReportsPage.tsx` — Component handles data fetching + export logic + filter state + loading states + rendering (SRP violation). Extract export logic into `useExportOrders` hook.
- **[SOLID-002]** `ReportFiltersBar.tsx:26` — `handleClear()` hardcodes all 9 filter field names (OCP violation). Use a typed empty filters constant instead.

### DRY

- **[DRY-003]** `ReportsPage.tsx` — Inline filter reset in `handleClear` should be a named constant `EMPTY_REPORT_FILTERS` exported from `domain/constants.ts`
- **[DRY-004]** Multiple files define order status list — consolidate to `ORDER_STATUS_LIST` in `constants.ts`

### Naming

- **[NAME-001]** `ReportTable.tsx:10` — `ORDER_ID_PREVIEW_LENGTH` is a local constant; should use camelCase: `orderIdPreviewLength`
- **[NAME-002]** `ReportsPage.tsx:35` — `ISO_DATE_LENGTH` is a local constant inside a function; should use camelCase: `isoDateLength`

### Bugs

- **[BUG-002]** `reportsApi.ts` — No error handling in `fetchReportOrders`; network failures surface as unhandled exceptions
- **[BUG-003]** `reportsApi.ts` — `as Promise<ReportOrdersResponse>` type assertion bypasses type safety
- **[BUG-004]** `exportOrdersToExcel.ts` — `URL.revokeObjectURL` not in `finally` block; memory leak if download throws
- **[BUG-005]** `ReportsPage.tsx` — Concurrent exports not prevented; rapid clicks can trigger multiple simultaneous downloads

### Security

- **[SEC-001]** `reportsApi.ts:8` — `buyerId` and `sellerId` exposed as URL query parameters (browser history, server logs)
- **[SEC-002]** `route.ts:67` — `amountMin`/`amountMax` not validated for negative values or non-numeric input before query construction
- **[SEC-003]** `route.ts:48` — `dateFrom`/`dateTo` used directly without ISO format validation

### Performance

- **[PERF-001]** `ReportsPage.tsx` — `currencies` array recomputed on every render; wrap in `useMemo`
- **[PERF-002]** `ReportsPage.tsx` — `handleFiltersChange` recreated on every render; wrap in `useCallback`
- **[PERF-003]** `ReportsPage.tsx` — `handleExport` recreated on every render; wrap in `useCallback`
- **[PERF-004]** `ReportTable.tsx` — `toLocaleDateString()` called per row per render; consider memoized formatting
- **[PERF-005]** `ReportTable.tsx` — `toLocaleString()` called per row per render; consider memoized formatting
- **[PERF-006]** `useReportOrders.ts` — Missing `staleTime` in `useQuery`; consider `staleTime: 30_000` (as done in audit feature)

### Patterns

- **[PAT-001]** `searchParams.ts` — Status enum array duplicates constants (same as DRY-001)
- **[PAT-002]** `useReportOrders.ts` — Missing `staleTime` (see PERF-006)
- **[PAT-003]** `reportsApi.ts` — `fetchReportOrders` accepts full `ReportFilters`; other features use `Partial<T>` for optional filters

### Testing

- **[TEST-007]** `ReportTable.tsx` — Inconsistent `tid` import (`"shared"` vs `"@/shared/infrastructure/config/tid"`)
- **[TEST-008]** `OrderStatusBadge.tsx` — Missing `tid()` attribute; not testable via test ID

---

## 🟡 Suggestions

- **[SOLID-003]** `OrderStatusBadge.tsx:8` — `STATUS_COLOR_MAP` uses `Record<string, string>` instead of `Partial<Record<OrderStatus, string>>`; status prop accepts `string` instead of `OrderStatus`
- **[SOLID-004]** `useReportOrders.ts:11` — Hook mixes data fetching (useQuery) with URL state (useQueryStates); could be split into `useReportFilters` + `useReportOrdersQuery` if it grows
- **[ARCH-003]** `reportsApi.ts:1` — Relative import `"../domain/types"` should be absolute for consistency: `"@/features/reports/domain/types"`
- **[COMP-001]** `ReportsPage.tsx` — `renderContent()` defined inline in component body; extract to named function or JSX variable
- **[STYLE-001]** `OrderStatusBadge.tsx` — Use `cn()` for class composition instead of template literal string
- **[DRY-005]** `ReportsPage.tsx` — Status list also available in constants; use imported constant directly
- **[PAT-004]** `route.ts` — `MAX_LIMIT=10000` is a magic number; move to `domain/constants.ts`
- **[BUG-006]** `route.ts` — `amountMin`/`amountMax` parsed with `Number()` which returns `NaN` for non-numeric inputs; use `parseFloat` + `isNaN` check

---

## ℹ️ Info

- **[ARCH-004]** Missing barrel `index.ts` files in `application/`, `domain/`, `infrastructure/`, `presentation/components/`
- **[ARCH-005]** `ISO_DATE_LENGTH = 10` is a magic number; could live in `domain/constants.ts`
- **[ARCH-006]** `ORDER_ID_PREVIEW_LENGTH = 8` could live in a `presentation/constants.ts`
- **[ARCH-007]** `MUTED_STYLE` and `WARNING_STYLE` in `OrderStatusBadge.tsx` are component-level style constants
- **[ARCH-008]** Excel XML constants in `exportOrdersToExcel.ts` could be extracted to `domain/constants.ts`
- **[SEC-004]** `receipt_url` rendered as href without URL scheme validation; low risk but could validate `https://` only
- **[SEC-005]** Excel export includes PII (emails, transfer numbers) without audit logging
- **[SEC-006]** No rate limiting on reports API with 10,000 row limit
- **[PERF-007]** Excel export creates DOM element for download (classic pattern, acceptable)
- **[PAT-005]** Good: XML/HTML escaping in `exportOrdersToExcel.ts`
- **[PAT-006]** Good: Defensive `STATUS_COLOR_MAP[status] ?? MUTED_STYLE` fallback in `OrderStatusBadge`
- **[PAT-007]** Good: Type-safe translation keys via `TranslationKeys` helper
- **[PAT-008]** Good: URL state management with nuqs correctly implemented in domain layer
- **[PAT-009]** Good: `getAuthorizedAdmin()` permission check with `admin.reports` scope
- **[PAT-010]** No cross-feature imports detected — feature isolation respected
- **[TEST-009]** E2E tests in `apps/admin/e2e/reports.spec.ts` provide good coverage
- **[STYLE-002]** All 156 Tailwind classes use semantic tokens correctly (excellent)
- **[STYLE-003]** No arbitrary color values found — full semantic color compliance
- **[STYLE-004]** Proper `cn()` usage in most components

---

## Pattern Discovery

### Undocumented Patterns Found

1. **XML-escaped Excel export** — `exportOrdersToExcel.ts` escapes all cell content (good pattern, document in team wiki)
2. **Composite status badge** — `OrderStatusBadge` maps status strings to semantic Tailwind tokens
3. **URL state as domain concern** — `searchParams.ts` lives in `domain/` layer (correct and consistent with nuqs strategy)

### New Rule Suggestions

1. Consider a rule: "Local function-scoped constants use camelCase; exported module-level constants use SCREAMING_SNAKE_CASE"
2. Consider documenting: `staleTime: 30_000` as the project default for non-real-time queries

---

## Action Items

- [x] E2E tests added (`apps/admin/e2e/reports.spec.ts`)
- [ ] Fix `item.id` React key bug (BUG-001) — CRITICAL
- [ ] Remove duplicate `ORDER_STATUS_VALUES` in searchParams.ts (DRY-001) — CRITICAL
- [ ] Extract row building in exportOrdersToExcel.ts (DRY-002) — CRITICAL
- [ ] Create unit tests for all 4 components, 1 hook, 1 utility (TEST-001–006) — CRITICAL
- [ ] Fix `tid` import in ReportTable.tsx (ARCH-001)
- [ ] Rename SCREAMING_SNAKE_CASE local constants (NAME-001, NAME-002)
- [ ] Add useMemo/useCallback for currencies, handleFiltersChange, handleExport (PERF-001–003)
- [ ] Add try/finally in downloadExcel (BUG-004)
- [ ] Add input validation for amounts and dates in API route (SEC-002, SEC-003)
- [ ] Add staleTime to useReportOrders (PAT-002/PERF-006)
- [ ] Add tid() to OrderStatusBadge (TEST-008)
- [ ] Type STATUS_COLOR_MAP properly (SOLID-003)
- [ ] Fix absolute import for reportsApi.ts (ARCH-003)
